% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/custom_neonstore.r
\name{neon_read_custom}
\alias{neon_read_custom}
\title{neon_read_custom}
\usage{
neon_read_custom(
  table = NA,
  product = NA,
  site = NA,
  start_date = NA,
  end_date = NA,
  ext = NA,
  timestamp = NA,
  dir = neonstore::neon_dir(),
  files = NULL,
  sensor_metadata = TRUE,
  altrep = FALSE,
  ...
)
}
\arguments{
\item{table, product, site, start_date, end_date, ext, timestamp}{Filters passed to the
file index (see \code{neonstore::neon_index}).}

\item{dir}{Local neonstore directory. Defaults to \code{neonstore::neon_dir()}.}

\item{files}{Optional explicit vector of file paths to read instead of indexing \code{dir}.}

\item{sensor_metadata}{If TRUE, extract domain/site/position/date columns from sensor filenames.}

\item{altrep}{Passed to \code{vroom::vroom}.}

\item{...}{Additional arguments passed to \code{vroom::vroom}.}
}
\value{
A tibble of the combined NEON data, or NULL if no files are found.
}
\description{
Read NEON data (including ragged-schema sensor products) from a local
  neonstore directory, de-duplicating files whose timestamps have been updated. A
  custom replacement for \code{neonstore::neon_read} that handles sensor metadata and
  inconsistent column schemas across months.
}
